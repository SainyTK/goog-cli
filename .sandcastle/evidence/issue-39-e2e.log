Issue 39 E2E evidence, redacted
Result: PARTIAL

Test setup:
- Accounts used: none; live Google credentials are unavailable in this sandbox.
- Fixture used: none.
- Skipped fixture: live Google Docs fixture skipped because `goog` is not installed on PATH and `.sandcastle/secrets/` contains no exported token/config files. Per docs/agents/e2e-testing.md, this means live E2E is unavailable and local checks continue.

Local checks:
$ command -v goog && goog auth list
Expected: list already-connected Google accounts for selecting an E2E account.
Observed: command failed because `goog` is not installed on PATH in this sandbox.
Result: PARTIAL.

$ cargo run --quiet -- docs list-images --help
Expected: command help renders for image listing.
Observed: help rendered with `goog docs list-images [OPTIONS] <DOCUMENT_ID>` and `--json`.
Result: PASS.

$ cargo run --quiet -- docs insert-image --help
Expected: command help renders for inline image insertion.
Observed: help rendered with `goog docs insert-image [OPTIONS] <DOCUMENT_ID> <IMAGE_URI>`, Location Selector flags, `--dry-run`, `--json`, and `--required-revision-id`.
Result: PASS.

$ cargo test docs_
Expected: parser, raw Docs behavior, Document Map, image listing, insert-image dry-run, and related high-level Docs regressions pass.
Observed: 66 docs-filtered tests passed plus 1 distribution doc test selected by the filter. Coverage includes documents with Inline Images and Positioned Images, image handles, positioned layout metadata, `insertInlineImage` request construction, and a human dry-run preview with `[inline image]` inserted in context.
Result: PASS.

$ npm run typecheck
Expected: repo typecheck passes.
Observed: cargo check passed.
Result: PASS.

$ npm run test
Expected: full repo test suite passes.
Observed: cargo test passed with 347 unit tests, 6 distribution artifact tests, and 0 doctests.
Result: PASS.

Live checks:
$ goog docs list-images "$DOC_ID"
$ goog docs list-images "$DOC_ID" --json
$ goog docs insert-image "$DOC_ID" "$IMAGE_URI" --index 1 --dry-run
$ goog docs insert-image "$DOC_ID" "$IMAGE_URI" --index 1 --dry-run --json
$ goog docs insert-image "$DOC_ID" "$IMAGE_URI" --index 1 --dry-run --json --required-revision-id "$REVISION_ID"
Expected: verify real Google Docs image listing and inline image insertion request shape against an already-connected Google account.
Observed: skipped because no exported Google credentials are available in `.sandcastle/secrets/` and no `goog` executable is installed.
Result: PARTIAL.

Reviewer notes:
- No real email addresses, document contents, tokens, or secrets were available or captured.
- `insert-image` accepts a publicly reachable image URI because Google Docs `insertInlineImage` requires `uri`; local file upload remains outside this issue.
- Human review should run the live commands in issue 39 Test steps with a scratch `goog-e2e-` Google Doc before closing the issue.
