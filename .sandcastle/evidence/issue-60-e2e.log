Issue 60 E2E evidence, redacted
Result: PASS

Test setup:
- Accounts used: Gmail account with existing mail access, redacted.
- Fixture used: no created fixture; read-only Gmail search query intentionally chosen to have zero matches.
- Skipped fixture: accounts that prompted for additional Gmail consent were not used.
- Note: `goog` was not installed on PATH in this sandbox, so live checks used the same local CLI binary through `cargo run --quiet --`.

Local checks:
$ cargo test search_messages_treats
Expected: Gmail list responses with null, missing, or empty `messages` data are treated as empty result sets.
Observed: 3 tests passed.
Result: PASS.

$ cargo test run_search
Expected: Existing mail search command rendering still works and empty search results produce explicit output.
Observed: 7 tests passed, including the empty table and empty JSON search command tests.
Result: PASS.

Live checks:
$ goog auth list
Expected: Confirm whether the installed `goog` binary is available.
Observed: command failed with `/bin/bash: line 1: goog: command not found`.
Result: PARTIAL.

$ cargo run --quiet -- auth list
Expected: Confirm the local CLI can see already-authorized accounts.
Observed: exit 0 with four redacted accounts listed.
Result: PASS.

$ cargo run --quiet -- --account [account-redacted] mail search "zzzzxyqqqnotexist12345" --limit 10
Expected: A zero-result Gmail search exits 0 and prints an explicit no-match message instead of a decode error or a bare table header.
Observed: exit 0 with `No matching messages found.`
Result: PASS.

$ cargo run --quiet -- --account [account-redacted] mail search "zzzzxyqqqnotexist12345" --limit 10 --json
Expected: A zero-result Gmail search in JSON mode exits 0 and prints an explicit empty JSON result.
Observed: exit 0 with `[]`.
Result: PASS.

Reviewer notes:
- This fix is scoped to Gmail message-list pages. Empty successful bodies for raw message reads or attachment payloads still remain invalid JSON.
- `mail search --json` still emits NDJSON rows for populated results; only the empty result case prints `[]`.
- No message subjects, snippets, senders, recipients, tokens, or message IDs were recorded.
